name = "hf-papers-chat"
main = "worker.js"
compatibility_date = "2024-01-01"

# After `wrangler deploy`, copy the printed URL into the PWA:
#   https://hf-papers-chat.<your-subdomain>.workers.dev
#
# Then run once:
#   wrangler secret put ANTHROPIC_KEY
